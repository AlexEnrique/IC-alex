/*
  Functions to implement:
    initialize(&lattice)
    floatSpins(&lattice)
    totalEnergy(&lattice)
    raffleRandomPosition(&pos)
    spinFlipped(pos, &lattice)
    adjustObservables(E, ...)
    sum(E)

 */

 #include <stdio.h>
 #include <stdlib.h>
 #include <math.h>
 #include <gsl/gsl_rng.h>
